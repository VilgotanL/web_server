vilgotanl.edeby.net {
	reverse_proxy localhost:3000

	respond /hi "Hello, world!"

	reverse_proxy /socket localhost:1234 {
		header_upstream Host {host} 
        header_upstream X-Real-IP {remote} 
        header_upstream X-Forwarded-For {remote} 
	}
}